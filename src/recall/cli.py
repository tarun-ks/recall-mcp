def main() -> None:
    raise NotImplementedError(
        "recall CLI is not yet implemented in this build "
        "(Phase 2 will add: recall index, recall eval, recall scrub-test, recall serve)."
    )
